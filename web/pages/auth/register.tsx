import { FunctionComponent } from "react";

import { Layout } from "components";

const RegisterPage: FunctionComponent = () => {
  return <Layout title="Register">Register page.</Layout>;
};

export default RegisterPage;
